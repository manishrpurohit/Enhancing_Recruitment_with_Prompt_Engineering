services:
  - type: web
    name: recruitment-prompt-engineering
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: DEFAULT_MODEL
        value: gemini-3.6-flash

